import { DragNDropItemDirective } from './drag-n-drop-item.directive';

describe('DragNDropItemDirective', () => {
    it('should create an instance', () => {
        const directive = new DragNDropItemDirective();
        expect(directive).toBeTruthy();
    });
});
