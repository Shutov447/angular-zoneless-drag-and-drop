import { DragNDropContainerDirective } from './drag-n-drop-container.directive';

describe('DragNDropContainerDirective', () => {
    it('should create an instance', () => {
        const directive = new DragNDropContainerDirective();
        expect(directive).toBeTruthy();
    });
});
